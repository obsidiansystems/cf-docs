import ExternalSpliceMainSpliceRstCodeDocsSrcSvOperatorSvHelmParsedLiteral354 from "/snippets/internal/splice/main/splice-rst-code-docs-src-sv-operator-sv-helm-parsed-literal-354.mdx";
import ExternalSpliceMainSpliceRstCodeDocsSrcSvOperatorSvHelmText366 from "/snippets/internal/splice/main/splice-rst-code-docs-src-sv-operator-sv-helm-text-366.mdx";

To generate the node config you use the CometBFT docker image provided through Github Container Registry (|docker_repo_prefix|).

Use the following shell commands to generate the proper keys:

<ExternalSpliceMainSpliceRstCodeDocsSrcSvOperatorSvHelmParsedLiteral354 />

Please keep a note of the node ID printed out above.

In addition, please retain some of the configuration files generated, as follows (you might need to change the permissions/ownership for them as they are accessible only by the root user):

<ExternalSpliceMainSpliceRstCodeDocsSrcSvOperatorSvHelmText366 />

Any other files can be ignored.
