---
title: "Smoke Test Fixture"
---

{/* COPIED_START source="testdata/fixtures/smoke.rst" hash="ee273063" */}

## Smoke Test Fixture

This fixture exercises the main rst-to-mdx transforms so the smoke
test can detect regressions end to end. Keep it small and intentional;
any change here must be matched by an intentional update to
`smoke.expected.mdx`.

### Headings and Inline

A short paragraph with **bold**, *italic*, and `inline code`.
See [another-anchor](#TODO-resolve-ref-another-anchor) for the cross-reference fallback behavior
(no docs-root is passed, so the label falls back to a TODO marker).

### Code Blocks

```bash
echo "hello smoke"
exit 0
```

### Tables

| Column A | Column B |
| --- | --- |
| one | two |
| three | four |

### Admonitions

<Note>
This is a note. It becomes a Mintlify `<Note>` component.
</Note>
### Images

![example smoke image](/images/docs_website/logo.png)

{/* COPIED_END */}
